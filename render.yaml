services:
  - type: web
    name: paypay-scratch-chance
    env: node
    buildCommand: echo "No build needed - using Node.js built-in modules"
    startCommand: node server-simple.js
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 16.20.0 